/**
 * Substituted by Vite at build time (design D58). They are literals in the bundle, so
 * reading them costs nothing and makes no request.
 */
declare const __APP_VERSION__: string
declare const __BUILT_AT__: string
